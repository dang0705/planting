# 浇水水量档位重构（Q2-B）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让盆型（体积+基质多选+排水孔）真实参与浇水算法，前端支持逐日期水量选择，输出算法建议档位（启用 mist/small）+ 用户历史剂量回显双轨。

**Architecture:** 后端先做纯函数层重构（pot-geometry 基质加权、hydration-load 排水孔修复+amountClass 体积分档+userDoseEcho 提取、watering-planner 返回 echo），全部 TDD 覆盖；再打通 plant-user-http 响应字段；最后前端删 DoseSelector、CareBehaviorTimeline 加逐日水量列表（prop 开关隔离诊断流）、WateringReminderSheet 双轨展示。

**Tech Stack:** Node.js 纯函数（CloudBase 云函数 layer）、自研断言测试、UniApp Vue3 组件。

关联 spec：`docs/superpowers/specs/2026-07-01-浇水水量档位重构-design.md`

---

## 文件结构

后端（先做，纯函数可独立测试）：
- `cloudfunctions/layer/utils/pot-geometry.js` — 基质因子表补全 + `resolveSubstrateRetentionFactor` 多选加权
- `cloudfunctions/layer/utils/hydration-load.js` — 排水孔 DRY 修复 + `computeAmountSuggestion` 体积分档 + `resolveUserDoseEcho`
- `cloudfunctions/layer/utils/watering-planner.js` — 返回 `userDoseEcho`
- `cloudfunctions/plant-user-http/app.js` — 响应补 `userDoseEcho`
- `test/unit-test/test-watering-planner-v21.mjs` — 扩充测试

前端（后端稳定后做）：
- `src/components/care-behavior-timeline/CareBehaviorWateringDoseList.vue` — 新增逐日水量列表
- `src/components/CareBehaviorTimeline.vue` — 加 `enableDosePerDate` prop
- `src/components/care-behavior-timeline/useCareBehaviorTimeline.js` — 日期→amount 映射
- `src/pages/index/components/WateringReminderSheet.vue` — 删 DoseSelector、双轨展示、弹窗按钮
- `src/pages/index/components/DoseSelector.vue` — 删除

文档：
- `docs/code-logics/12_浇水提醒算法_逐步逻辑_盆型与Gate.md` — 同步

---

## Task 1: 基质因子表补全 + 多选加权解析

**Files:**
- Modify: `cloudfunctions/layer/utils/pot-geometry.js`
- Test: `test/unit-test/test-watering-planner-v21.mjs`

- [ ] **Step 1: 写失败测试**

require 块把 `pot-geometry` 解构补上 `resolveSubstrateRetentionFactor`：

```js
const { computePotGeometry, resolveSubstrateRetentionFactor } = require('../../cloudfunctions/layer/utils/pot-geometry.js')
```

盆型几何测试区追加：

```js
test('substrate: 新增排水基质因子命中非默认值', () => {
  assert.ok(resolveSubstrateRetentionFactor('perlite') < 0.6, 'perlite 保水应偏低')
  assert.ok(resolveSubstrateRetentionFactor('ceramsite') <= 0.5, 'ceramsite 保水应偏低')
  assert.ok(resolveSubstrateRetentionFactor('coarse_sand') < 0.6, 'coarse_sand 保水应偏低')
})

test('substrate: JSON 数组多选按比例加权', () => {
  const single = resolveSubstrateRetentionFactor('peat')
  const mixed = resolveSubstrateRetentionFactor(
    JSON.stringify([{ material: 'peat', ratio: 50 }, { material: 'perlite', ratio: 50 }])
  )
  assert.ok(mixed < single && mixed > resolveSubstrateRetentionFactor('perlite'),
    '混合保水因子应介于纯泥炭与纯珍珠岩之间')
})

test('substrate: 非法 JSON / 空 → 1.0 基线', () => {
  assert.equal(resolveSubstrateRetentionFactor('unknown'), 1.0)
  assert.equal(resolveSubstrateRetentionFactor('[bad json'), 1.0)
  assert.equal(resolveSubstrateRetentionFactor(''), 1.0)
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node test/unit-test/test-watering-planner-v21.mjs`
Expected: FAIL（`resolveSubstrateRetentionFactor is not a function`）

- [ ] **Step 3: 实现**

`SUBSTRATE_TYPE` 枚举补三项：

```js
const SUBSTRATE_TYPE = Object.freeze({
  GENERAL: 'general',
  PEAT: 'peat',
  COCO: 'coco',
  BARK: 'bark',
  SPHAGNUM: 'sphagnum',
  GRITTY: 'gritty',
  PERLITE: 'perlite',
  CERAMSITE: 'ceramsite',
  COARSE_SAND: 'coarse_sand',
  UNKNOWN: 'unknown'
})
```

`SUBSTRATE_RETENTION_FACTOR` 补三项（排水材料，保水低）：

```js
const SUBSTRATE_RETENTION_FACTOR = Object.freeze({
  general: 1.0,
  peat: 1.3,
  coco: 1.2,
  bark: 0.7,
  sphagnum: 1.4,
  gritty: 0.5,
  perlite: 0.4,
  ceramsite: 0.5,
  coarse_sand: 0.4,
  unknown: 1.0
})
```

`normalizeSubstrateType` 后新增：

```js
/**
 * 解析基质保水因子，支持单值枚举或 JSON 数组字符串（多选+比例）。
 * JSON 数组元素形如 { material, ratio }（ratio 为百分数）；按 ratio 加权平均。
 */
function resolveSubstrateRetentionFactor(substrateType) {
  const raw = String(substrateType ?? '').trim()
  if (raw.startsWith('[')) {
    let parsed
    try {
      parsed = JSON.parse(raw)
    } catch {
      return 1.0
    }
    if (!Array.isArray(parsed) || !parsed.length) {
      return 1.0
    }
    let weightSum = 0
    let factorSum = 0
    for (const item of parsed) {
      const material = normalizeSubstrateType(item?.material)
      const ratio = Number(item?.ratio)
      if (!Number.isFinite(ratio) || ratio <= 0) {
        continue
      }
      const factor = SUBSTRATE_RETENTION_FACTOR[material] ?? 1.0
      weightSum += ratio
      factorSum += factor * ratio
    }
    return weightSum > 0 ? factorSum / weightSum : 1.0
  }
  const single = normalizeSubstrateType(raw)
  return SUBSTRATE_RETENTION_FACTOR[single] ?? 1.0
}
```

`computePotGeometry` 里把 `const substrateRetentionFactor = SUBSTRATE_RETENTION_FACTOR[substrateType] ?? 1.0` 改为：

```js
const substrateRetentionFactor = resolveSubstrateRetentionFactor(
  potProfile.substrateType ?? potProfile.substrate_type
)
```

`module.exports` 补 `resolveSubstrateRetentionFactor`。

- [ ] **Step 4: 运行测试确认通过**

Run: `node test/unit-test/test-watering-planner-v21.mjs`
Expected: PASS（新 3 个 + 原有全绿）

- [ ] **Step 5: 提交**

```bash
git add cloudfunctions/layer/utils/pot-geometry.js test/unit-test/test-watering-planner-v21.mjs
git commit -m "feat(watering): 基质多选按比例加权保水因子 + 补全排水基质因子表"
```

---

## Task 2: 排水孔 DRY 判定阈值修复

**Files:**
- Modify: `cloudfunctions/layer/utils/hydration-load.js`（约 358 行 `isTooLongAgo`）
- Test: `test/unit-test/test-watering-planner-v21.mjs`

- [ ] **Step 1: 写失败测试**

```js
test('gate: DRY 阈值真正受 baselineIntervalDays[0] 约束', () => {
  const gate = evaluateDryWetGate({
    rootZoneMoistureIndex: 0.2,
    wetPressureLoad: 0,
    lastEffectiveRootWateredDaysAgo: 3,
    potGeometry: { hasDrainageHole: 'true' },
    weatherWetPressureHitCount: 0,
    forecastHotDryHit: false,
    historicalHotDryHit: false,
    baselineIntervalDays: [10, 14],
    recentThoroughWatering: false
  })
  assert.notEqual(gate.gateState, GATE_STATE.DRY, '未达 baseline min 天数不应判 DRY')
})
```

- [ ] **Step 2: 运行确认失败**

Run: `node test/unit-test/test-watering-planner-v21.mjs`
Expected: FAIL（`|| 5` 恒真导致误判 DRY）

- [ ] **Step 3: 实现**

定位：

```js
  const isTooLongAgo =
    lastEffectiveRootWateredDaysAgo === null ||
    lastEffectiveRootWateredDaysAgo >= Number(baselineIntervalDays[0]) || 5
```

改为：

```js
  const baselineMinDays = Number(baselineIntervalDays[0]) || 5
  const isTooLongAgo =
    lastEffectiveRootWateredDaysAgo === null ||
    lastEffectiveRootWateredDaysAgo >= baselineMinDays
```

- [ ] **Step 4: 运行确认通过**

Run: `node test/unit-test/test-watering-planner-v21.mjs`
Expected: PASS（含原有 DRY 用例无回归）

- [ ] **Step 5: 提交**

```bash
git add cloudfunctions/layer/utils/hydration-load.js test/unit-test/test-watering-planner-v21.mjs
git commit -m "fix(watering): 修复 DRY 判定 baseline min 阈值被 ||5 恒真旁路的缺陷"
```

---

## Task 3: amountClass 体积分档（启用 mist/small）

**Files:**
- Modify: `cloudfunctions/layer/utils/hydration-load.js`（`computeAmountSuggestion`，新增 `classifyDoseByAmount`）
- Test: `test/unit-test/test-watering-planner-v21.mjs`

- [ ] **Step 1: 写失败测试**

```js
test('amountSuggestion: 小盆体积 → 档位落 mist/small', () => {
  const smallGeo = computePotGeometry({
    potTopDiameterCm: 6, potBottomDiameterCm: 5, potHeightCm: 6,
    hasDrainageHole: 'true', potMaterial: 'plastic', substrateType: 'general'
  })
  const sug = computeAmountSuggestion(smallGeo, GATE_STATE.DRY)
  assert.ok([DOSE_CLASS.MIST, DOSE_CLASS.SMALL].includes(sug.amountClass),
    `小盆 DRY 应落 mist/small，实际 ${sug.amountClass}（上限 ${sug.amountRangeMl[1]}ml）`)
})

test('amountSuggestion: 大盆 DRY → thorough', () => {
  const bigGeo = computePotGeometry({
    potTopDiameterCm: 30, potBottomDiameterCm: 24, potHeightCm: 28,
    hasDrainageHole: 'true', potMaterial: 'ceramic', substrateType: 'general'
  })
  const sug = computeAmountSuggestion(bigGeo, GATE_STATE.DRY)
  assert.equal(sug.amountClass, DOSE_CLASS.THOROUGH)
})

test('amountSuggestion: 无盆型保守 normal 不猜档', () => {
  const dry = computeAmountSuggestion({}, GATE_STATE.DRY)
  assert.equal(dry.amountClass, DOSE_CLASS.NORMAL)
  assert.equal(dry.confidenceLevel, 'low')
  const base = computeAmountSuggestion({}, GATE_STATE.BASELINE)
  assert.equal(base.amountClass, DOSE_CLASS.NORMAL)
})

test('amountSuggestion: WET 恒暂停', () => {
  const bigGeo = computePotGeometry({
    potTopDiameterCm: 30, potBottomDiameterCm: 24, potHeightCm: 28, hasDrainageHole: 'true'
  })
  const sug = computeAmountSuggestion(bigGeo, GATE_STATE.WET)
  assert.deepEqual(sug.amountRangeMl, [0, 0])
})
```

- [ ] **Step 2: 运行确认失败**

Run: `node test/unit-test/test-watering-planner-v21.mjs`
Expected: FAIL（小盆当前恒 thorough）

- [ ] **Step 3: 实现**

替换 `computeAmountSuggestion` 整个函数体为：

```js
function computeAmountSuggestion(potGeometry = {}, gateState = GATE_STATE.BASELINE, _baselineIntervalDays = [5, 8]) {
  const volumeMl = Number(potGeometry.potVolumeMl) || 0
  const volumeConfidence = potGeometry.volumeConfidence || 'low'

  if (volumeMl <= 0) {
    if (gateState === GATE_STATE.WET) {
      return { amountClass: DOSE_CLASS.UNKNOWN, amountRangeMl: [0, 0], stopCondition: '暂停浇水，检查土壤干湿后再决定', confidenceLevel: 'low' }
    }
    if (gateState === GATE_STATE.DRY) {
      return { amountClass: DOSE_CLASS.NORMAL, amountRangeMl: [100, 200], stopCondition: '盆底出水即可停止', confidenceLevel: 'low' }
    }
    return { amountClass: DOSE_CLASS.NORMAL, amountRangeMl: [50, 150], stopCondition: '盆土表面湿润即可停止', confidenceLevel: 'low' }
  }

  if (gateState === GATE_STATE.WET) {
    return { amountClass: DOSE_CLASS.UNKNOWN, amountRangeMl: [0, 0], stopCondition: '暂停浇水，检查土壤干湿后再决定', confidenceLevel: volumeConfidence }
  }

  let amountRangeMl
  let stopCondition
  if (gateState === GATE_STATE.DRY) {
    amountRangeMl = [Math.round(volumeMl * 0.2), Math.round(volumeMl * 0.3)]
    stopCondition = '盆底有水流出即可停止'
  } else {
    amountRangeMl = [Math.round(volumeMl * 0.1), Math.round(volumeMl * 0.15)]
    stopCondition = '盆土表面湿润即可停止'
  }
  const amountClass = classifyDoseByAmount(amountRangeMl[1])

  return { amountClass, amountRangeMl, stopCondition, confidenceLevel: volumeConfidence }
}

/**
 * 按单次建议水量上限（ml）落档：≤30 mist / ≤80 small / ≤300 normal / >300 thorough。
 */
function classifyDoseByAmount(upperMl = 0) {
  const ml = Number(upperMl) || 0
  if (ml <= 30) { return DOSE_CLASS.MIST }
  if (ml <= 80) { return DOSE_CLASS.SMALL }
  if (ml <= 300) { return DOSE_CLASS.NORMAL }
  return DOSE_CLASS.THOROUGH
}
```

- [ ] **Step 4: 运行确认通过**

Run: `node test/unit-test/test-watering-planner-v21.mjs`
Expected: PASS。若原有测试 `planner: 盆型档案影响水量建议` / `amountSuggestion: DRY 时建议浇透` 硬断言 amountClass 具体值，改为区间/成员断言（例如断言 `amountRangeMl[1] > amountRangeMl[0]` 与档位属于合法枚举），不再硬编码 thorough。

- [ ] **Step 5: 提交**

```bash
git add cloudfunctions/layer/utils/hydration-load.js test/unit-test/test-watering-planner-v21.mjs
git commit -m "feat(watering): amountClass 按盆型体积分档，启用 mist/small 档位"
```

---

## Task 4: userDoseEcho 用户历史剂量回显

**Files:**
- Modify: `cloudfunctions/layer/utils/hydration-load.js`（新增 `resolveUserDoseEcho` + export）
- Modify: `cloudfunctions/layer/utils/watering-planner.js`（引入 + result 补 `userDoseEcho`）
- Test: `test/unit-test/test-watering-planner-v21.mjs`

- [ ] **Step 1: 写失败测试**

require 块补 `resolveUserDoseEcho`（加入 hydration-load 解构）。追加：

```js
test('userDoseEcho: 取最近一次非喷雾剂量', () => {
  const events = [makeEvent(1, 'mist'), makeEvent(2, 'thorough'), makeEvent(5, 'small')]
  assert.equal(resolveUserDoseEcho(events, REF_DATE), DOSE_CLASS.THOROUGH)
})

test('userDoseEcho: 只有喷雾 → mist', () => {
  assert.equal(resolveUserDoseEcho([makeEvent(1, 'mist')], REF_DATE), DOSE_CLASS.MIST)
})

test('userDoseEcho: 无事件 → null', () => {
  assert.equal(resolveUserDoseEcho([], REF_DATE), null)
})

test('planner: 返回 userDoseEcho', () => {
  const plan = buildWateringPlanner({
    wateringStrategy: { freq: [5, 8] },
    behaviorTimeline: normalizeCareBehaviorTimeline({
      referenceDate: REF_DATE,
      watering_events_10d: [makeEvent(2, 'thorough')]
    }),
    potProfile: { potTopDiameterCm: 12, potBottomDiameterCm: 8, potHeightCm: 10, hasDrainageHole: 'true' },
    referenceDate: REF_DATE
  })
  assert.equal(plan.userDoseEcho, DOSE_CLASS.THOROUGH)
})
```

- [ ] **Step 2: 运行确认失败**

Run: `node test/unit-test/test-watering-planner-v21.mjs`
Expected: FAIL（`resolveUserDoseEcho is not a function`）

- [ ] **Step 3: 实现**

`hydration-load.js` 在 `computeLastEffectiveRootWateredDaysAgo` 后新增：

```js
/**
 * 提取用户近期代表性剂量：最近一次非喷雾浇水的 doseClass；
 * 若只有喷雾则返回 mist；无事件返回 null。
 */
function resolveUserDoseEcho(wateringEvents = [], referenceDate = '') {
  let bestDiff = null
  let bestDose = null
  let mistSeen = false
  for (const event of wateringEvents) {
    const doseClass = resolveDoseClass(event)
    if (doseClass === DOSE_CLASS.MIST) {
      mistSeen = true
      continue
    }
    const diff = referenceDate ? daysAgo(referenceDate, event.date) : 0
    const effectiveDiff = diff === null ? Number.MAX_SAFE_INTEGER : diff
    if (bestDiff === null || effectiveDiff < bestDiff) {
      bestDiff = effectiveDiff
      bestDose = doseClass
    }
  }
  if (bestDose !== null) {
    return bestDose
  }
  return mistSeen ? DOSE_CLASS.MIST : null
}
```

`module.exports` 补 `resolveUserDoseEcho`。

`watering-planner.js`：确认顶部从 `./hydration-load` 解构处补 `resolveUserDoseEcho`（若用整体 require 则用 `hydrationLoad.resolveUserDoseEcho`——按文件现有引入风格）。在 `buildWateringPlanner` 算出 `amountSuggestion` 后、组装 `result` 前新增：

```js
  const userDoseEcho = resolveUserDoseEcho(
    Array.isArray(behaviorTimeline.watering_events_10d)
      ? behaviorTimeline.watering_events_10d
      : (timeline.watering_events_10d || []),
    referenceDate
  )
```

result 对象在 `rootZoneMoistureIndex,` 后补：

```js
    userDoseEcho,
```

- [ ] **Step 4: 运行确认通过**

Run: `node test/unit-test/test-watering-planner-v21.mjs`
Expected: PASS（全绿）

- [ ] **Step 5: 提交**

```bash
git add cloudfunctions/layer/utils/hydration-load.js cloudfunctions/layer/utils/watering-planner.js test/unit-test/test-watering-planner-v21.mjs
git commit -m "feat(watering): 新增 userDoseEcho 用户历史剂量回显"
```

---

## Task 5: plant-user-http 响应透出 userDoseEcho

**Files:**
- Modify: `cloudfunctions/plant-user-http/app.js`（watering-planner 响应 data）

- [ ] **Step 1: 实现（纯字段透传，端上验收覆盖）**

在响应 data 中 `rootZoneMoistureIndex: plan.rootZoneMoistureIndex` 后补一行（注意加逗号）：

```js
          rootZoneMoistureIndex: plan.rootZoneMoistureIndex,
          userDoseEcho: plan.userDoseEcho
```

- [ ] **Step 2: 语法校验**

Run: `node --check cloudfunctions/plant-user-http/app.js`
Expected: 无输出（语法正确）

- [ ] **Step 3: 提交**

```bash
git add cloudfunctions/plant-user-http/app.js
git commit -m "feat(watering): watering-planner 响应透出 userDoseEcho"
```

---

## Task 6: 后端 lint + 全量测试关卡

- [ ] **Step 1: oxlint 后端改动文件**

Run: `npx oxlint cloudfunctions/layer/utils/pot-geometry.js cloudfunctions/layer/utils/hydration-load.js cloudfunctions/layer/utils/watering-planner.js cloudfunctions/plant-user-http/app.js`
Expected: 0 errors

- [ ] **Step 2: 全量浇水单测**

Run: `node test/unit-test/test-watering-planner-v21.mjs`
Expected: 全部 `✓`

- [ ] **Step 3: schema 契约测试无回归**

Run: `node --test test/unit-test/test-watering-reminder-v21-schema-contract.mjs`
Expected: PASS

---

## Task 7: CareBehaviorTimeline 逐日水量列表子组件

**Files:**
- Create: `src/components/care-behavior-timeline/CareBehaviorWateringDoseList.vue`
- Modify: `src/components/CareBehaviorTimeline.vue`
- Modify: `src/components/care-behavior-timeline/useCareBehaviorTimeline.js`

- [ ] **Step 1: 新建列表子组件**

创建 `src/components/care-behavior-timeline/CareBehaviorWateringDoseList.vue`：

```vue
<template>
  <view v-if="rows.length" class="care-dose-list mt-3 rounded-[12px] border border-gray-100 bg-white p-3">
    <text class="block text-sm font-medium text-gray-900">每次浇了多少水？</text>
    <text class="mt-0.5 block text-xs text-gray-400">喷雾不计入根区浇水，浇透会提高过浇风险</text>
    <view v-for="row in rows" :key="row.date" class="mt-3">
      <view class="flex items-center justify-between">
        <text class="text-xs font-medium text-gray-700">{{ row.date }}</text>
        <text class="text-[10px] text-[#2f8f57]">{{ doseLabel(row.amount) }}</text>
      </view>
      <view class="mt-2 flex items-center justify-between">
        <view
          v-for="opt in doseOptions"
          :key="opt.value"
          class="flex flex-col items-center"
          @click="selectDose(row.date, opt.value)"
        >
          <view
            class="flex items-center justify-center rounded-full"
            :class="row.amount === opt.value ? 'size-5 bg-[#2f8f57]' : 'size-3 bg-gray-200'"
          >
            <view v-if="row.amount === opt.value" class="size-2 rounded-full bg-white" />
          </view>
          <text
            class="mt-1 text-[10px]"
            :class="row.amount === opt.value ? 'text-[#2f8f57] font-medium' : 'text-gray-400'"
          >{{ opt.label }}</text>
        </view>
      </view>
    </view>
  </view>
</template>

<script setup>
defineProps({
  rows: { type: Array, default: () => [] }
})
const emit = defineEmits(['update-dose'])

const doseOptions = [
  { label: '不知道', value: 'unknown' },
  { label: '喷雾', value: 'mist' },
  { label: '少量', value: 'small' },
  { label: '普通', value: 'normal' },
  { label: '浇透', value: 'thorough' }
]

function doseLabel(value) {
  return doseOptions.find(o => o.value === value)?.label || '普通'
}

function selectDose(date, value) {
  emit('update-dose', { date, amount: value })
}
</script>
```

- [ ] **Step 2: composable 维护 date→amount 映射**

先确认 `useCareBehaviorTimeline.js` 顶部已 `import { ref, computed } from 'vue'`（缺 `computed` 则补）。

新增响应式（与其它 ref 同级）：

```js
  const wateringDoseByDate = ref({})

  function setWateringDose(date, amount) {
    if (!date) { return }
    wateringDoseByDate.value = { ...wateringDoseByDate.value, [date]: amount }
  }
```

在构造 `selected_watering_events_10d`（约 219 行）处改为带 amount 覆盖：

```js
      selected_watering_events_10d: (nextTimeline.watering_events_10d || []).map(ev => ({
        ...ev,
        amount: wateringDoseByDate.value[ev.date] || ev.amount || 'normal'
      }))
```

新增供列表渲染的 computed（放在 `timelinePayload` 定义之后）：

```js
  const wateringDoseRows = computed(() =>
    (timelinePayload.value?.selected_watering_events_10d || []).map(ev => ({
      date: ev.date,
      amount: ev.amount || 'normal'
    }))
  )
```

在 return 对象补：`wateringDoseByDate,` `setWateringDose,` `wateringDoseRows,`。

- [ ] **Step 3: CareBehaviorTimeline.vue 接入 prop 与子组件**

`defineProps` 补：`enableDosePerDate: { type: Boolean, default: false }`。

template 在 `<CareBehaviorTimelineLegend />` 后加：

```vue
    <CareBehaviorWateringDoseList
      v-if="enableDosePerDate"
      :rows="wateringDoseRows"
      @update-dose="onUpdateDose"
    />
```

script 引入子组件、从 composable 解构 `wateringDoseRows` `setWateringDose`，新增：

```js
import CareBehaviorWateringDoseList from './care-behavior-timeline/CareBehaviorWateringDoseList.vue'

function onUpdateDose({ date, amount }) {
  setWateringDose(date, amount)
}
```

- [ ] **Step 4: lint**

Run: `npx oxlint src/components/care-behavior-timeline/CareBehaviorWateringDoseList.vue src/components/CareBehaviorTimeline.vue src/components/care-behavior-timeline/useCareBehaviorTimeline.js`
Expected: 0 errors

- [ ] **Step 5: 提交**

```bash
git add src/components/care-behavior-timeline/CareBehaviorWateringDoseList.vue src/components/CareBehaviorTimeline.vue src/components/care-behavior-timeline/useCareBehaviorTimeline.js
git commit -m "feat(watering): CareBehaviorTimeline 逐日水量列表（enableDosePerDate 隔离诊断流）"
```

---

## Task 8: WateringReminderSheet 删 DoseSelector + 双轨展示

**Files:**
- Modify: `src/pages/index/components/WateringReminderSheet.vue`
- Delete: `src/pages/index/components/DoseSelector.vue`

- [ ] **Step 1: 弹窗启用逐日水量、移除 DoseSelector 标签**

日期选择弹窗（`datePickerPopupRef`）内：给 `<CareBehaviorTimeline>` 加 `:enable-dose-per-date="true"`；删除 `<DoseSelector v-model="selectedDoseClass" @cancel="closeDatePicker" @confirm="confirmDatePicker" />`；在 CareBehaviorTimeline 下补按钮容器：

```vue
        <view class="mx-4 mt-2 flex gap-3 pb-3">
          <button
            class="m-0 flex-1 rounded-[10px] border border-gray-200 bg-white py-2.5 text-sm text-gray-700 after:border-0"
            hover-class="none"
            @click="closeDatePicker"
          >取消</button>
          <button
            class="m-0 flex-1 rounded-[10px] bg-[#2d7a4f] py-2.5 text-sm font-medium text-white after:border-0"
            hover-class="none"
            @click="confirmDatePicker"
          >确认</button>
        </view>
```

- [ ] **Step 2: 移除 selectedDoseClass 与 DoseSelector 引用**

删 `import DoseSelector from './DoseSelector.vue'`；删 `const selectedDoseClass = ref('normal')`；删 `watch(() => props.plant?.id, ...)` 里的 `selectedDoseClass.value = 'normal'` 一行。

- [ ] **Step 3: 双轨展示 —— plannerSummaryRows 增回显行**

在 `plannerSummaryRows` computed 里，`if (plannerResult.value?.confidenceLevel)` 块之后、`return rows` 之前追加：

```js
  if (plannerResult.value?.userDoseEcho) {
    const echoMap = { unknown: '不确定', mist: '喷雾', small: '少量', normal: '普通', thorough: '浇透' }
    rows.push({
      label: '你通常浇',
      value: echoMap[plannerResult.value.userDoseEcho] || '普通',
      valueClass: 'text-xs text-gray-500'
    })
  }
```

- [ ] **Step 4: 删除 DoseSelector.vue**

```bash
git rm src/pages/index/components/DoseSelector.vue
```

- [ ] **Step 5: lint**

Run: `npx oxlint src/pages/index/components/WateringReminderSheet.vue`
Expected: 0 errors，无 `selectedDoseClass`/`DoseSelector` 残留引用

- [ ] **Step 6: 提交**

```bash
git add src/pages/index/components/WateringReminderSheet.vue
git commit -m "feat(watering): 删除全局 DoseSelector，逐日水量 + 建议/你通常浇双轨展示"
```

---

## Task 9: 同步算法文档 + 端上验收

**Files:**
- Modify: `docs/code-logics/12_浇水提醒算法_逐步逻辑_盆型与Gate.md`

- [ ] **Step 1: 更新文档现状**

- §3.2 基质：标注 JSON 数组多选已按 ratio 加权（`resolveSubstrateRetentionFactor`），补全 perlite/ceramsite/coarse_sand 因子；删除"JSON 数组会被归一为 unknown"的已知局限段。
- §5 gate：删除"`|| 5` 恒真"缺陷记录，改为"已修复，baseline min 阈值生效"。
- §6 amountClass：更新为体积分档表（≤30 mist/≤80 small/≤300 normal/>300 thorough），删除"mist/small 是死代码""恒为 normal"结论；补 userDoseEcho 双轨输出说明。
- §8 可审计性：标注响应已透出 `userDoseEcho`。

- [ ] **Step 2: 提交文档**

```bash
git add docs/code-logics/12_浇水提醒算法_逐步逻辑_盆型与Gate.md
git commit -m "docs(watering): 同步算法文档至 Q2-B 重构后现状"
```

- [ ] **Step 3: 端上验收（项目规则 14）**

Run: `npm run dev:mp-weixin:local-functions:lan`（完整 LAN 本地函数 flow，让小程序运行时命中新代码）

验证项：
1. 浇水提醒弹窗选多个日期 → 逐日水量列表出现对应行，每行可独立调 5 档 slider。
2. 调不同水量 → 确认页"建议水量"随盆型/gate 变化（不再恒 normal），小盆能出现少量/喷雾档。
3. 已设盆型基质多选的植物 → 建议随基质变化（保水强的建议更保守）。
4. "你通常浇 X"回显行出现且与所选历史剂量一致。
5. 诊断流回归：进入黄叶/发蔫问诊的水分行为时间线，交互与提交与改造前一致（无逐日水量列表，payload 无异常）。

- [ ] **Step 4: 端上验收通过后收尾提交（如有 dist 或残留改动）**

```bash
git status
```

---

## 自审记录

- **Spec 覆盖**：改动1-8 对应 spec §5.1-5.8；测试对应 spec §6；文档对应 spec 第7节。全覆盖。
- **类型一致性**：`resolveSubstrateRetentionFactor`（Task1）、`classifyDoseByAmount`（Task3）、`resolveUserDoseEcho`（Task4）三处新函数命名在测试与实现、export、调用点一致；`userDoseEcho` 字段名从 hydration-load → watering-planner result → app.js 响应 → 前端 plannerResult 全链一致；`enableDosePerDate` prop 名在 CareBehaviorTimeline 定义与 WateringReminderSheet 传参一致。
- **placeholder 扫描**：无 TBD/TODO，每个 code step 均含完整代码。
- **诊断流兼容**：`enableDosePerDate` 默认 false 保护诊断流；`userDoseEcho` 为追加字段不改既有语义；Task9 Step3 含诊断流回归验证项。
