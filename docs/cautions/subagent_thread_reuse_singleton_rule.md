# Subagent 复用规则（历史记录）

更新时间：2026-04-22

## 当前规则入口

当前正式规则已迁移到 `docs/ai-rules/subagent-thread-reuse.md`。本文件只保留历史执行记录和旧规则来源，不作为主工作流入口。

## 历史规则

1. 同一角色（包括 `new-rules-keeper`、`modulize-keeper`、`dent-guard`、`review` 等）在同一会话内必须复用同一个 subagent 线程。
2. 不允许并行启动同角色的多个线程。
3. 当需要继续该角色任务时，优先复用已存在的对应线程；若该线程失效，再重新启动同角色的新线程并关闭旧线程的残留。
4. 如确有特殊场景必须重开，必须在闭环前写明原因与替换依据。

## 本次执行记录

- 已关闭额外子线程：
  - `019db435-7b87-7cb0-a11d-ae3f2a0390d6`
  - `019db44f-ee24-7d12-96a4-3e9dd665484f`
  - `019db45d-703c-73d3-b0d4-0f4f15789216`
- 关闭后将按单线程复用策略继续主任务推进。
