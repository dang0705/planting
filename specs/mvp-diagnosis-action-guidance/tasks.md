# MVP 诊断建议与规避动作实施任务

状态约定：`[ ]` 未开始，`[~]` 进行中，`[x]` 已完成，`BLOCKED_ENV` 表示环境或权限阻断。

## 1. 事实源与权威资料登记

- [x] 1.1 以当前源码和 `cloud1_dev` 实际数据冻结 27 个终端诊断对象清单，确认未把 `root_rot` 计入 MVP。
  - 产出对象清单、现有 `actionProfileKey` 映射和缺口报告。
  - _Requirement: R1, R6_
- [x] 1.2 建立权威来源登记及来源 ID 校验规则，逐个覆盖虫害、病害、黄叶、发蔫和环境/营养问题。
  - 产出来源机构、URL、主题、核对日期和可引用范围。
  - _Requirement: R2, R5_

## 2. 动作分类与结构化合同

- [x] 2.1 新增纯函数动作分类字典，固定 11 个 `categoryId` 及其用户可见中文名。
  - 强制保留 `pest_kill_treatment`（杀虫/杀螨处理）、`disease_kill_treatment`（杀菌处理）和 `treatment_safety`（用药注意事项）。
  - _Requirement: R3_
- [x] 2.2 新增结构化动作解析、校验、阶段映射和 ID 去重模块。
  - 校验动作 ID 唯一、分类合法、阶段合法、实施方式存在、来源 ID 已登记。
  - 从结构化动作生成已有文本数组，保持历史调用方可用。
  - _Requirement: R3, R4, R6_
- [x] 2.3 为动作分类、动作结构和数据缺陷补充后端单元测试。
  - 覆盖空数据、重复 ID、未知分类、无来源、煤污病误用杀菌、红蜘蛛缺少杀螨分类等失败路径。
  - _Requirement: R6_

## 3. 数据合同与内容补齐

- [x] 3.1 扩展现有 `outcome_action_profiles` 导入配置和读取映射，增加 `action_items_json`。
  - 不新增动作表；保留旧五个阶段字段作为历史数据兼容来源。
  - _Requirement: R4_
- [x] 3.2 准备并审核共享动作画像。
  - 覆盖螨类、刺吸式害虫、蓟马、潜叶虫、蕈蚊、白粉病、煤污病，以及现有黄叶/发蔫画像。
  - 相似处理共享画像；生活部位、处理方式或风险不同的对象保留独立画像。
  - _Requirement: R2, R3, R5_
- [x] 3.3 为 27 个终端对象补齐 `diagnosis_outcomes.action_profile_key`。
  - 八种虫害、白粉病、煤污病必须有画像；17 个现有通用结果不得丢失原映射。
  - _Requirement: R1, R4_
- [x] 3.4 在明确的 `cloud1_dev` 环境中执行字段变更和数据写入，并记录 SQL、影响行数、数据快照和回读结果。
  - 实际回读：27 个终端对象全部有画像，27 个画像均有结构化动作且包含规避动作；共复用 21 个动作画像。
  - 字段级审计：102 条结构化动作均有 ID、分类、阶段、实施方式、文案和来源；无非法字段、无画像内重复 ID。
  - 变更前检查表结构和唯一键；变更后只允许审计通过的数据进入运行时。
  - _Requirement: R4, R5, R6_

## 4. 后端结果链路接入

- [x] 4.1 让动作画像读取同时返回结构化 `actionItems` 和兼容文本数组。
  - 保留 `actionAdviceItems`、`avoidAdviceItems` 等已有字段。
  - _Requirement: R4_
- [x] 4.2 接通虫害直判结果。
  - `specific-pest-answer-resolver` 输出八种虫害对应的画像、动作分类、实施步骤和规避动作。
  - 红蜘蛛结果必须包含“杀虫/杀螨处理”。
  - _Requirement: R1, R3, R4_
- [x] 4.3 接通白粉病和煤污病直判结果。
  - 白粉病必须包含“杀菌处理”。
  - 煤污病默认走查找蜜露害虫、物理清洁和复查，不默认输出“杀菌处理”。
  - _Requirement: R1, R2, R3_
- [x] 4.4 保持黄叶和发蔫已有结果行为，并验证共享画像按 `actionProfileKey + action.id` 合并去重。
  - 冲突动作继续触发当前冲突保护，不同时推送互相冲突的处理建议。
  - _Requirement: R4, R6_
- [x] 4.5 更新后端响应压缩和公开字段映射，保证结构化动作不会被压扁成 `[object Object]` 或静默丢失。
  - _Requirement: R4, R5_

## 5. 前端建议展示

- [x] 5.1 更新诊断结果动作分组，优先以 `categoryNameCn` 显示分类标题。
  - 用户可见“杀虫/杀螨处理”“杀菌处理”“用药注意事项”等白话分类。
  - 动作 ID和来源 ID保留在响应审计字段，不直接堆在普通用户界面。
  - _Requirement: R3, R5_
- [x] 5.2 保持旧文本数组和无结构化动作结果的显示兼容。
  - _Requirement: R4_
- [x] 5.3 补充前端动作分类、合并、规避分组的单元测试。
  - _Requirement: R6_

## 6. 分层验证与收口

- [x] 6.1 运行前端/后端相关单元测试，确认结构化动作和旧合同均通过。
  - 测试报告标记 `unit_fake`、`unit_real_data` 等数据模式，不把单元测试写成端上验收。
  - 本次相关测试通过；部分旧路由测试因本机缺少 `/opt/utils/cloudbase` 依赖，在 require 阶段标记 `BLOCKED_ENV`。
  - _Requirement: R6_
- BLOCKED_ENV 6.2 运行真实 API E2E，使用真实 `cloud1_dev` 和真实配置核对 27 个对象的画像、分类、ID、来源和关键动作文本。
  - 缺少真实 API、身份或开发环境时标记 `BLOCKED_ENV`，不得回退 fixture。
  - 已完成 `cloud1_dev` 数据库真实回读审计，但本轮未部署变更后的 HTTP 函数，因此不能替代真实 API E2E。
  - _Requirement: R6_
- BLOCKED_ENV 6.3 按项目规定跑真实微信端上链路，至少验证红蜘蛛、白粉病、煤污病和一个黄叶/发蔫结果页的用户可见建议与规避动作。
  - 证据必须包含项目 identity、页面数据、有效截图和真实 `wx.request` 请求记录。
  - _Requirement: R5, R6_
- [x] 6.4 执行 CloudBase 代码审查，复核环境边界、数据写入、敏感信息、回滚路径和缓存合同。
  - 已复核 CloudBase 函数无完整 headers/env/context 回显；本次未新增认证、存储或表，字段变更有独立 SQL，且可按 SQL 指纹回退。
  - _Requirement: R4, R6_
- [x] 6.5 更新本文件状态、记录已覆盖和未覆盖项，形成最终审计报告。
  - _Requirement: R6_

## 当前执行门

任务拆分已确认，正式实现已完成。当前未覆盖项只有变更后 HTTP 函数的真实 API E2E 和真实微信端上验收；两项因本轮未部署/未建立完整端上链路标记为 `BLOCKED_ENV`，不能宣称全链路验收通过。
