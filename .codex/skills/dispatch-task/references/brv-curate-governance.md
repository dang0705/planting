# ByteRover V4 Record Governance — 旧名称兼容入口

本文件仅用于兼容仍指向 `brv-curate-governance.md` 的旧引用。

正式规范：

```text
references/brv-record-governance.md
```

规则：

1. 不得从本文件执行 V3 `brv curate`、`brv search`、swarm、manifest 或本地 Context Tree 工作流。
2. 本文件不定义任何 BRV 内容边界；内容资格唯一由项目 `AGENTS.md` 裁决。
3. 发现调用方仍引用本文件时，应迁移到 `brv-record-governance.md`，不得长期维护两份规范。
