thread_id: 019fa2f1-f587-7922-b807-44e7b1c35db8
updated_at: 2026-07-27T12:42:04+00:00
rollout_path: /Users/jay/.codex/sessions/2026/07/27/rollout-2026-07-27T17-39-53-019fa2f1-f587-7922-b807-44e7b1c35db8.jsonl
cwd: /Users/jay/WebstormProjects/planting
git_branch: sprint-ai-workflow

# Diagnosis routing investigation followed by a completed user-plants identity repair

Rollout context: `/Users/jay/WebstormProjects/planting`. The rollout first investigated why a single `aphid` candidate at confidence `0.95` could still produce a frontend outcome, then shifted to a concrete local LAN `user-plants` defect where literal `"null"` records occupied the first page.

## Task 1: Diagnose single aphid candidate and `visimg1_*` ID

Outcome: partial

Key steps:
- Traced `mode_candidates → diagnosis-mode-router → pest-visual-orchestrator → frontend response`.
- Confirmed `mode_candidates` are normalized with per-candidate confidence and routed by confidence tier.
- For a single high-confidence visual-direct pest candidate, the router may select `direct_result`; frontend outcomes are built from `visibleOutcomes`/`finalResult`, not directly from the raw AI log.
- Confirmed `visimg1_<timestamp>_<random>` is generated by `buildRuntimeId('visimg1')` in `cloudfunctions/diagnose-http/services/visual-diagnosis-service.js`, so it is a runtime image identifier rather than evidence of a Codex review mutation.
- Recent commits `37bf16f`, `2257d06`, `e0682a5`, and `5e7716e` changed routing, confidence propagation, and outcome shaping, but the rollout did not prove that this specific ID or outcome was caused by those changes.

Reusable knowledge:
- Single-candidate behavior must be analyzed through `diagnosis-mode-router.js`, `pest-visual-orchestrator.js`, `specific-pest-answer-resolver.js`, and `frontend-response-helpers.js`; raw `aiDebug` alone is insufficient.
- `visimg1_*` IDs are generated from timestamp plus random suffix and are expected to vary per runtime image.

## Task 2: Repair local `user-plants` list identity and remove bogus null cards

Outcome: success

Preference signals:
- The user expected the existing LAN/startup flow to be preserved and did not want database cleanup or fabricated data. Future fixes should prefer read-layer filtering and restart the existing process only when needed.
- The user required valid records to appear immediately on the first page, with `total` and `hasMore` consistent with the same filtering predicate.

Key steps:
- Added local/LAN auth precedence in `src/http-functions/core/httpRequest.js`: use `VITE_DEV_OPENID` directly before storage access; preserve prior non-local fallback behavior.
- Added displayability filtering in `cloudfunctions/layer/utils/plant-knowledge.js`: a row is displayable if at least one of `plant_id`, `plant_identity_id`, `session_plant_id`, `canonical_name`, `recognized_name`, or `nickname` is nonempty and not literal `null`/`undefined`.
- Applied the same SQL predicate to list and count, plus a defensive in-memory filter before mapping. No database rows were deleted, updated, or migrated.
- Added mirrored backend unit coverage.
- Focused frontend and backend tests passed; scoped oxlint passed with 0 errors and 58 warnings. The npm lint wrapper was incompatible with the installed CLI because `--files` was unsupported.
- Restarted using the original `npm run dev:mp-weixin:local-functions:lan` flow. LAN probe returned HTTP 200 with `total: 4`, IDs `[12,9,8,7]`, and no null display names. DevTools showed four named plants on the home page.
- Completion readiness, episode finish, and trace audit all passed.

References:
- Changed files: `src/http-functions/core/httpRequest.js`, `test/unit/frontend/http-functions/core/httpRequest.mjs`, `cloudfunctions/layer/utils/plant-knowledge.js`, `test/unit/backend/layer/utils/plant-knowledge-user-plants.mjs`.
- Runtime command: `npm run dev:mp-weixin:local-functions:lan`.
- Verification: `user-plants` HTTP 200, `total=4`, `hasMore=false`; Completion Gate status `passed`.
