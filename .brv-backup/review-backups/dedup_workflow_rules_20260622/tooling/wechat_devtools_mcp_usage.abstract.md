# Abstract — WeChat DevTools MCP Usage Memory

Use this context when tasks mention WeChat DevTools MCP, mini-program automation, `Transport closed`, mp-weixin, miniprogram-ci, automator, or端上验收. It contains source-verified package/config facts plus dispatch/recovery rules that must be propagated through `subagent_memory_context` and `wechat_mcp_policy_context`.
